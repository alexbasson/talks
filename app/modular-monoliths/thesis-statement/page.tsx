export default function Page() {
  return (
    <div className={'text-lg text-center px-[300px] py-[150px]'}>
      <p>
        by focusing less on <span className={"text-yellow-600 italic"}>services</span> and more on <span className={"text-yellow-600 italic"}>boundaries</span>, we can reap <span className={"text-yellow-600"}>early rewards</span> with <span className={"text-yellow-600"}>less cost</span>, and without blocking ourselves off from the
        microservices we will <span className={"text-yellow-600"}>eventually need</span>
      </p>
    </div>
  )
}
